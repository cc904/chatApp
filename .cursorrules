# Cursor项目规范 - Flutter WhatsApp 克隆项目

## 基本技术栈
- Flutter框架
- Cubit状态管理
- Isar数据库
- Socket.io客户端
- Integration_test测试框架

## 基本设置
- 使用中文回答所有问题
- 按照Sequential Thinking方法进行代码设计和实现

## 项目结构和命名规范
- 组件命名：PascalCase
- 函数命名：camelCase
- 常量命名：UPPER_CASE
- 文件夹结构遵循：/lib/components, /lib/services, /lib/utils, /docs
- 接口命名：使用I前缀，如IUserService
- 类型定义：使用T前缀，如TUserData
- Cubit命名：功能+Cubit，如ChatCubit
- 模型命名：功能+Model，如MessageModel

## 代码风格
- 使用Dart类型定义
- 统一代码缩进和格式
- 使用Flutter Linter规则
- 遵循Flutter官方编码风格指南

## 开发流程规则
1. 问题分析：明确任务目标和约束条件
2. 设计方案：设计数据结构和算法
3. 实现步骤：自顶向下实现功能
4. 测试验证：编写单元测试和集成测试

## 代码审查标准
- 代码必须符合既定规范
- 关键函数和组件必须有注释
- 复杂算法需要解释思路

## 提交规范
- 遵循语义化提交信息格式：feat:, fix:, docs:等
- 主分支只接受经过测试的代码

## 文档要求
- API接口必须有清晰文档
- 更新README文件反映最新项目状态

## 代码稳定性保障
- 修改已完成功能前必须先理解其完整设计意图
- 所有API更改必须向下兼容
- 使用单元测试保护核心功能逻辑
- 重构代码时必须保持功能等价性
- 在修改前创建功能快照或临时分支
- 每次更改后必须验证不破坏现有功能
- 使用TODO或FIXME标签清晰标记未完成修改

## 版本控制规则
- 使用语义化版本管理
- 关键功能更改必须通过代码审查
- 维护变更日志记录所有修改
- 为已稳定功能模块添加"锁定注释"标记：/* @stable - 请勿修改 */

## UI设计规范
- 遵循WhatsApp设计风格
- 使用Flutter Material Design组件库作为基础
- 实现响应式设计，确保在不同设备上显示良好
- 使用主题系统统一管理颜色、字体、间距等设计标记
- 优先采用Flex布局系统
- 确保适当的留白和视觉层次
- 支持深色模式

## UI组件标准
- 使用Flutter内置组件作为基础
- 自定义组件需符合WhatsApp风格
- 设计组件应包含默认、悬停、聚焦、禁用等状态
- 使用适当的动画和过渡效果增强用户体验
- 确保设计一致性：同类元素使用相同样式
- 使用ThemeData支持亮色/暗色模式切换
- 根据用户操作提供视觉反馈

## 依赖管理规范
- 优先使用国内镜像站点安装依赖
- pub.dev包使用国内镜像：https://pub.flutter-io.cn
- 在pubspec.yaml中明确指定依赖版本号
- 安装新依赖前先验证其在国内是否可访问
- 如确实需使用国外资源，应提供备选方案或离线安装包
- 记录所有依赖的具体版本和来源以便追踪

## 状态管理规范
- 统一使用Cubit作为状态管理方案
- 区分本地状态和全局状态
- 避免状态冗余和重复存储
- 实现不可变状态更新模式
- 为复杂状态提供初始值和验证机制
- Cubit状态应该是不可变的
- 每个功能模块拥有独立的Cubit

## 数据库规范
- 使用Isar作为本地数据库
- 定义清晰的数据模型和关系
- 实现数据库迁移策略
- 封装数据库操作为仓库模式(Repository)
- 处理并发访问和事务

## 网络通信规范
- 使用Socket.io与Next.js后端通讯
- 实现消息重试和错误处理机制
- 处理网络状态变化和断线重连
- 消息格式应统一且支持版本化
- 敏感数据传输必须加密

## 测试自动化规范
- 使用integration_test作为主要测试框架
- 编写单元测试覆盖所有关键功能
- 实现集成测试验证用户流程
- 使用测试驱动开发(TDD)方法
- 每次提交前运行自动化测试
- 维护测试数据与生产环境隔离

## 调试与日志规范
- 统一使用日志级别（error, warn, info, debug）
- 日志信息需包含上下文和时间戳
- 生产环境禁用调试代码和print语句
- 关键流程增加日志记录点便于问题追踪
- 使用结构化日志格式便于分析

## 代码复杂度控制
- 函数不超过50行，单个文件不超过300行
- 每个函数只做一件事情，保持单一职责
- 嵌套不超过3层，避免过深条件嵌套
- 控制圈复杂度不超过10
- 复杂逻辑应拆分为多个小函数

## 异步操作规范
- 统一使用async/await
- 实现请求超时和重试机制
- 处理并发请求限制
- 取消不必要的请求以节省资源
- 使用Loading状态指示异步操作进行中

## 代码重用策略
- 提取通用逻辑为工具函数
- 使用组合而非继承实现代码复用
- 避免复制粘贴代码，而应重构为共享组件
- 通用功能应考虑发布为内部包
- 明确区分业务逻辑和技术实现

## 项目文档体系
- 维护项目架构图和关键流程图
- 记录技术选型理由和限制条件
- 为API和数据模型提供详细文档
- 记录已知问题和解决方案
- 提供新开发者入门指南

## 构建与发布流程
- 使用CI/CD实现自动化构建和部署
- 区分开发、测试、预发布和生产环境
- 实现回滚机制应对紧急问题
- 使用版本化静态资源避免缓存问题
- 配置文件应随环境变化而不需修改代码

## 可访问性和兼容性规范
- 确保键盘可访问性
- 使用语义化widgets增强屏幕阅读器支持
- 确保适当的颜色对比度
- 响应式设计支持从移动到平板的所有设备
- 针对低带宽和弱网络场景优化

## 性能优化指南
- 优化启动时间和首屏加载速度
- 实现图片和资源懒加载
- 使用缓存策略减少网络请求
- 避免不必要的重建widget树
- 使用const构造器优化内存使用
- 定期进行性能监测和优化

## 安全规范
- 敏感数据本地存储必须加密
- 实现安全的用户认证机制
- 使用安全的Socket.io连接
- 实现适当的数据过期和销毁机制
- 防止SQL注入和XSS攻击

## WhatsApp特定功能规范
- 聊天界面应支持文本、图片、语音、视频等多种消息类型
- 实现消息状态跟踪（发送中、已发送、已送达、已读）
- 支持群组聊天和一对一聊天
- 提供联系人管理功能
- 实现消息通知和提醒
- 支持媒体文件的预览和下载
- 实现简单的用户资料管理
